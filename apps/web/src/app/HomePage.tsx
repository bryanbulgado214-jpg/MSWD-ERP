/**
 * Temporary placeholder landing page. Replace once the first real
 * business module (or a proper ERP shell/dashboard) is built.
 */
export function HomePage() {
  return (
    <main style={{ fontFamily: 'system-ui, sans-serif', padding: '2rem' }}>
      <h1>MSWD ERP</h1>
      <p>Project foundation is running. No business modules installed yet.</p>
    </main>
  );
}
